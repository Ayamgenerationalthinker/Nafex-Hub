{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const f of ['package-lock.json','yarn.lock']) { try { fs.rmSync(f,{force:true}); } catch {} } const ua=process.env.npm_config_user_agent||''; const execPath=(process.env.npm_execpath||'').toLowerCase(); const isPnpm=ua.startsWith('pnpm/')||execPath.includes('pnpm'); if(!isPnpm){ console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "dev:web": "pnpm --filter @workspace/nafex-hub run dev",
    "dev:api": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/api-server run start",
    "db:push": "pnpm --filter @workspace/db run push",
    "typecheck:libs": "tsc --build --force",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm --filter @workspace/api-server --filter @workspace/nafex-hub run test"
  },
  "private": true,
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^7.2.0",
    "jest": "^30.4.2",
    "prettier": "^3.8.1",
    "supertest": "^7.2.2",
    "typescript": "~5.9.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/react-i18next": "^8.1.0",
    "cookie-parser": "^1.4.7",
    "esbuild": "0.27.3",
    "i18next": "^26.3.1",
    "i18next-browser-languagedetector": "^8.2.1",
    "i18next-http-middleware": "^3.9.7",
    "jsonwebtoken": "^9.0.3",
    "pino": "^9.14.0",
    "rollup": "^4.62.2",
    "zod": "catalog:"
  },
  "resolutions": {
    "esbuild": "0.27.3"
  }
}
